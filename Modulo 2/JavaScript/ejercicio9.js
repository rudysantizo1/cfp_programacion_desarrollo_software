let areaCirculo = radio => area = Math.PI * Math.pow(radio,2);
let circuferencia = radio => circulo = 2 * Math.PI * radio;


let radio = 5;
console.log(`Area del circulo ${areaCirculo(radio)}\n
            Circuferencia del circulo ${circuferencia(radio)}`);

