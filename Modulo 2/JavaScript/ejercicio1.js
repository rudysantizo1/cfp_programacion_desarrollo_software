function suma (x,y){
    console.log("La suma de los numeros es " + (x+y));
}

function resta(x,y) {
    console.log("La resta de los numeros es " + (x-y));
}

function multiplicacion(x,y) {
    console.log("La multiplicacion de los numeros es " + (x*y));
}

function division(x,y) {
    console.log("La division de los numeros es " + (x/y));
}

let num1 = 8, num2 = 4;

suma(num1,num2);
resta(num1,num2);
multiplicacion(num1,num2);
division(num1,num2);